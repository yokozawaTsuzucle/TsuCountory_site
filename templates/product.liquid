  <script type="text/javascript">
  window.product = {{product | json}} || {};
</script>


  
  <main class="product Contents-wrap">
  {% comment %} バリエーションの実装 {% endcomment %}
  {% assign current_variant = product.selected_or_first_available_variant %}
  {% assign featured_image = current_variant.featured_image | default: product.featured_image %}

  <section class="sec sec03">
    <div class="popular_product flex">
      <div class="product__post">

        <div class="ProductImage">

          <ul class="gallery">
{% for image in product.images %}
<li><img src="{{ image | image_url: width: 1000 }}" alt=""></li>
{% endfor %}
          </ul>

          <ul class="choice-btn flex slideItemImage">
{% for image in product.images %}
<li><img src="{{ image | image_url: width: 1000x }}" alt=""></li>
{% endfor %}

          </ul>
        </div>
        
        
      </div>

        <div class="product__detail">
          <div class="product__detail__stickey">
  
            <div class="product__detail__main">
              
              <h2>{{ product.title }}</h2>
              <p class="price">{{ product.price | money }}(税込)
              </p>
            </div>  
  












 {% comment %} <form method="post" action="/cart/add" enctype="multipart/form-data" id="AddToCartForm">
                <div class="counter">
                  <label for="number">数量</label>
                      <div class="line_item_count">
                          <!-- <div class="minus_btn">-</div>1<div class="plus_btn">+</div> -->
                          <input type="number" name="updates[]" class="btn btn-default" value="1" min="1" pattern="[0-9]*"max="{{ product.variants.first.inventory_quantity }}" >
                      </div>
                </div>
{% for product_option in product.options_with_values %}
  <label>
    {{ product_option.name }}
    <select>
      {% for value in product_option.values %}
        <option {% if product_option.selected_value == value %}selected{% endif %}>
          {{ value }}
        </option>
      {% endfor %}
    </select>
  </label>
{% endfor %}                    
              <input type="hidden" id="variant-id" name="id" value={{product.selected_or_first_available_variant.id}}>
              	<button type="submit" class="Btn02 bgleft02" name="add" id="AddToCart" ><span>カートに入れる</span></button>              
</form> {% endcomment %}








{% comment %} オプションからバリエーションを作成したい {% endcomment %}

          <form method="post" action="{{ routes.cart_add_url }}" {{ html_params }}>
            {% if product.variants.size > 1 %}
                {% for option in product.options %}
            	<label for="SingleOptionSelector-{{forloop.index0}}"> {{ option }} </label>
            	<select id="SingleOptionSelector-{{forloop.index0}}" class="single-option-selector single-option-selector-product-template product-form__input" data-index="option{{forloop.index}}" name="id">
    				{% capture option_index %}option{{ forloop.index }}{% endcapture %} 
    				{% assign option_values = product.variants | map: option_index | uniq %}
                    {% for choice in option_values %}
                 		<option value="{{ choice }}"> {{ choice }} </option>
                  	{% endfor %}
            	</select>
            
            	{% endfor %}
            	<select id="ProductSelect-product-template" class="product-form__variants no-js" name="id" data-productid="{{product.id}}">
                  	{% for option in product.variants %}
                 		 <option value="{{option.id}}"> {{option.title}}</option>
                  	{% endfor %}
            	</select>
            {% endif %}
            
  			 <div class="counter">
                  <label for="number">数量</label>
                      <div class="line_item_count">
                          <!-- <div class="minus_btn">-</div>1<div class="plus_btn">+</div> -->
                          <input type="number" name="updates[]" class="btn btn-default" value="1" min="1" pattern="[0-9]*"max="{{ product.variants.first.inventory_quantity }}" >
                      </div>
                </div>
                
        <button type="submit" class="Btn02 bgleft02" name="add" id="AddToCart" ><span>カートに入れる</span></button>   
		</form>

{% comment %} ここまで {% endcomment %}



            <div class="product__detail__text">
              {{ product.content }}
            </div>


          


               
                    
                    


                    
                    




              
        </div>
      </div>
    </div>

  </section>



  
  



</main>